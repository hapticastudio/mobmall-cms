<div class="row small-3">
  <p>
    Name: <%= @local.name %>
  </p>
  <p>
    Description: <%= @local.description.html_safe %>
  </p>
  <%= link_to "Edit", edit_local_path(@local), class: "button expand" %>
  <%= link_to "New event", new_local_event_path(@local), class: "button expand" %>
</div>
