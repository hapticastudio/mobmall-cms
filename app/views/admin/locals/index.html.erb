<div class="row small-3">
  <h1 class="text-center">Listing Locals</h1>
  <table>
    <thead>
      <td>Name</td>
      <td>Moderator</td>
      <td>Poi</td>
      <td>Edit</td>
    </thead>
    <tbody>
      <% @locals.each do |local| %>
        <tr>
          <td><%= local.name %></td>
          <td><%= local.moderator_email %></td>
          <td><%= local.poi %></td>
          <td><%= link_to "Edit local", edit_admin_local_path(local) %><td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
